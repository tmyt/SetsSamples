<Window x:Class="SetsSample.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SetsSample.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Click="EnableSets">Enable Sets</Button>
            <Button Click="DisableSets">Disable Sets</Button>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1">
            <Button Click="OpenTabDisabled">OpenTabDisabled</Button>
            <Button Click="OpenDefault">OpenDefault</Button>
            <Button Click="OpenTab">OpenTab</Button>
            <Button Click="OpenWindow">OpenWindow</Button>
        </StackPanel>
    </Grid>
</Window>
